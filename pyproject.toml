[project]
name = "watsit"
version = "0.1.0"
description = "Notification server for Raspberry Pi Zero W with e-ink display, audio, and LEDs"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "pillow>=12.1.1",
]
